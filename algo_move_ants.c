//
// Created by Peggie Donnie on 2019-12-01.
//
/*
#include "validator.h"

void 	move_ants(t_data *data, t_room **array, int **paths)
{

	if (l_ant != NULL)
		while (l_ant != NULL && locate->free)
		ft_move(l_ant->next);
		l_ant--;
	if (l_ant->locate != last)
	{
		l_ant->locate++;
		ft_move(l_ant);
	}

}
 */